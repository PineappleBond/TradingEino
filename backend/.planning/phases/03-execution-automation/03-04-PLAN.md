---
phase: 03-execution-automation
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - internal/agent/tools/okx_batch_place_order.go
  - internal/agent/tools/okx_batch_cancel_order.go
  - internal/agent/tools/okx_get_order_history.go
  - internal/agent/tools/okx_close_position.go
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03]

must_haves:
  truths:
    - "User can place multiple orders in single batch call (max 20)"
    - "User can cancel multiple orders in single batch call (max 20)"
    - "User can query historical orders with time range filter"
    - "User can close position partially or fully by percentage"
  artifacts:
    - path: "internal/agent/tools/okx_batch_place_order.go"
      provides: "Batch place order tool (max 20 orders)"
      exports: ["OkxBatchPlaceOrderTool", "NewOkxBatchPlaceOrderTool"]
    - path: "internal/agent/tools/okx_batch_cancel_order.go"
      provides: "Batch cancel order tool (max 20 orders)"
      exports: ["OkxBatchCancelOrderTool", "NewOkxBatchCancelOrderTool"]
    - path: "internal/agent/tools/okx_get_order_history.go"
      provides: "Get order history tool with time range"
      exports: ["OkxGetOrderHistoryTool", "NewOkxGetOrderHistoryTool"]
    - path: "internal/agent/tools/okx_close_position.go"
      provides: "Close position tool with percentage support"
      exports: ["OkxClosePositionTool", "NewOkxClosePositionTool"]
  key_links:
    - from: "internal/agent/tools/okx_batch_place_order.go"
      to: "pkg/okex/api/rest/trade.go"
      via: "OKXClient.Rest.Trade.PlaceMultipleOrders"
      pattern: "PlaceMultipleOrders"
    - from: "internal/agent/tools/okx_batch_cancel_order.go"
      to: "pkg/okex/api/rest/trade.go"
      via: "OKXClient.Rest.Trade.CandleOrder"
      pattern: "CandleOrder.*len.*>.*1"
    - from: "internal/agent/tools/okx_get_order_history.go"
      to: "pkg/okex/api/rest/trade.go"
      via: "OKXClient.Rest.Trade.GetOrderHistory"
      pattern: "GetOrderHistory"
    - from: "internal/agent/tools/okx_close_position.go"
      to: "pkg/okex/api/rest/trade.go"
      via: "OKXClient.Rest.Trade.ClosePosition"
      pattern: "ClosePosition"
---

<objective>
Implement P1 batch operations and advanced order tools: batch place, batch cancel, order history, and close position.

Purpose: Enable efficient multi-order operations and position management — batch operations reduce API calls, close position provides one-click exit.

Output: Four Tool implementations with rate limiting, partial failure handling for batch ops, and Markdown table responses.
</objective>

<execution_context>
@/Users/leichujun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leichujun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-execution-automation/03-CONTEXT.md
@.planning/phases/03-execution-automation/03-RESEARCH.md
@.planning/phases/03-execution-automation/03-VALIDATION.md

@internal/agent/tools/okx_candlesticks.go
@internal/agent/tools/okx_get_positions.go
@pkg/okex/api/rest/trade.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement okx-batch-place-order-tool</name>
  <files>internal/agent/tools/okx_batch_place_order.go, internal/agent/tools/okx_batch_place_order_test.go</files>
  <behavior>
    - Test 1: Place 2 orders in batch returns results for both
    - Test 2: Place 21 orders returns error (exceeds max 20)
    - Test 3: Batch with partial failures returns both successes and failures
    - Test 4: Empty orders array returns error
  </behavior>
  <action>
    Create `internal/agent/tools/okx_batch_place_order.go` for batch order placement:

    1. Define `OkxBatchPlaceOrderTool` struct with `svcCtx` and `limiter` (5 req/s)
    2. Implement `NewOkxBatchPlaceOrderTool(svcCtx)`
    3. Implement `Info(ctx)` returning ToolInfo with name `okx-batch-place-order` and parameters:
       - `orders` (array, required): Array of order objects, each containing:
         - `instID` (string): 交易对
         - `side` (string): buy/sell
         - `posSide` (string): long/short/net
         - `ordType` (string): market/limit/post_only/fok/ioc
         - `size` (string): 订单数量
         - `price` (string, optional): 价格 (limit/post_only 必填)
    4. Implement `InvokableRun(ctx, argsJSON)`:
       - Call `c.limiter.Wait(ctx)` before API call
       - Parse JSON arguments
       - Validate len(orders) <= 20, return error if exceeds
       - Validate len(orders) > 0, return error if empty
       - Call `c.svcCtx.OKXClient.Rest.Trade.PlaceMultipleOrders(...)`
       - Validate `resp.Code != 0` → return error
       - Iterate through results, categorize by SCode:
         - Successes: SCode == 0
         - Failures: SCode != 0
       - Format output with two sections:
         - "## 成功订单" table (ordId, instId, side, state)
         - "## 失败订单" table (请求索引，sCode, sMsg)
  </action>
  <verify>
    <automated>go test ./internal/agent/tools/... -run TestOkxBatchPlaceOrder -v</automated>
  </verify>
  <done>okx-batch-place-order-tool.go created with 20-order limit validation, partial failure handling</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement okx-batch-cancel-order-tool</name>
  <files>internal/agent/tools/okx_batch_cancel_order.go, internal/agent/tools/okx_batch_cancel_order_test.go</files>
  <behavior>
    - Test 1: Cancel 2 orders in batch returns results for both
    - Test 2: Cancel 21 orders returns error (exceeds max 20)
    - Test 3: Batch with partial failures returns both successes and failures
    - Test 4: Empty order IDs array returns error
  </behavior>
  <action>
    Create `internal/agent/tools/okx_batch_cancel_order.go` for batch order cancellation:

    1. Define `OkxBatchCancelOrderTool` struct with `svcCtx` and `limiter` (5 req/s)
    2. Implement `NewOkxBatchCancelOrderTool(svcCtx)`
    3. Implement `Info(ctx)` returning ToolInfo with name `okx-batch-cancel-order` and parameters:
       - `orders` (array, required): Array of cancel request objects, each containing:
         - `instID` (string): 交易对
         - `ordID` (string): 订单 ID
    4. Implement `InvokableRun(ctx, argsJSON)`:
       - Call `c.limiter.Wait(ctx)` before API call
       - Parse JSON arguments
       - Validate len(orders) <= 20, return error if exceeds
       - Validate len(orders) > 0, return error if empty
       - Call `c.svcCtx.OKXClient.Rest.Trade.CandleOrder(...)` (handles batch when len > 1)
       - Validate `resp.Code != 0` → return error
       - Iterate through results, categorize by SCode
       - Format output with "## 成功取消" and "## 失败取消" tables
  </action>
  <verify>
    <automated>go test ./internal/agent/tools/... -run TestOkxBatchCancelOrder -v</automated>
  </verify>
  <done>okx-batch-cancel-order-tool.go created with 20-order limit, partial failure handling</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement okx-get-order-history-tool</name>
  <files>internal/agent/tools/okx_get_order_history.go, internal/agent/tools/okx_get_order_history_test.go</files>
  <behavior>
    - Test 1: Query order history returns list of historical orders
    - Test 2: Query with time range filter returns filtered results
    - Test 3: Query with instID filter returns orders for specific instrument
    - Test 4: Empty result set returns empty list (not error)
  </behavior>
  <action>
    Create `internal/agent/tools/okx_get_order_history.go` for querying historical orders:

    1. Define `OkxGetOrderHistoryTool` struct with `svcCtx` and `limiter` (5 req/s)
    2. Implement `NewOkxGetOrderHistoryTool(svcCtx)`
    3. Implement `Info(ctx)` returning ToolInfo with name `okx-get-order-history` and parameters:
       - `instID` (string, optional): 交易对筛选，留空表示所有
       - `startTime` (string, optional): 开始时间（Unix 毫秒时间戳）
       - `endTime` (string, optional): 结束时间（Unix 毫秒时间戳）
       - `limit` (number, optional): 返回数量限制，默认 100
    4. Implement `InvokableRun(ctx, argsJSON)`:
       - Call `c.limiter.Wait(ctx)` before API call
       - Parse JSON arguments
       - Call `c.svcCtx.OKXClient.Rest.Trade.GetOrderHistory(...)`
       - Validate `resp.Code != 0` → return error
       - Format output as Markdown table with columns:
         ordId, instId, side, posSide, ordType, size, avgPx, fillSize, state, cTime
  </action>
  <verify>
    <automated>go test ./internal/agent/tools/... -run TestOkxGetOrderHistory -v</automated>
  </verify>
  <done>okx-get-order-history-tool.go created with time range filtering</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement okx-close-position-tool</name>
  <files>internal/agent/tools/okx_close_position.go, internal/agent/tools/okx_close_position_test.go</files>
  <behavior>
    - Test 1: Close 100% of position returns success
    - Test 2: Close 50% of position places opposite market order for half size
    - Test 3: Close position with no open position returns error
    - Test 4: Invalid percentage (< 0 or > 100) returns error
  </behavior>
  <action>
    Create `internal/agent/tools/okx_close_position.go` for closing positions:

    1. Define `OkxClosePositionTool` struct with `svcCtx` and `limiter` (5 req/s)
    2. Implement `NewOkxClosePositionTool(svcCtx)`
    3. Implement `Info(ctx)` returning ToolInfo with name `okx-close-position` and parameters:
       - `instID` (string, required): 交易对
       - `posSide` (string, optional): 仓位方向 long/short/net，留空查询全部
       - `percentage` (number, optional): 平仓百分比 0-100，默认 100
    4. Implement `InvokableRun(ctx, argsJSON)`:
       - Call `c.limiter.Wait(ctx)` before API call
       - Parse JSON arguments
       - Validate percentage in [0, 100], return error if invalid
       - If percentage == 100:
         - Call `c.svcCtx.OKXClient.Rest.Trade.ClosePosition(...)`
       - If percentage < 100:
         - First call `c.svcCtx.OKXClient.Rest.Account.GetPositions(...)` to get current position size
         - Calculate order size = positionSize * (percentage / 100)
         - Call `c.svcCtx.OKXClient.Rest.Trade.PlaceOrder(...)` with opposite side market order
       - Validate response, check SCode
       - Format output as Markdown table: instId, posSide, closedSize, percentage
  </action>
  <verify>
    <automated>go test ./internal/agent/tools/... -run TestOkxClosePosition -v</automated>
  </verify>
  <done>okx-close-position-tool.go created with percentage support</done>
</task>

</tasks>

<verification>
**Wave 3 Verification (after all tasks complete):**

1. All four tools compile:
   ```bash
   go build ./internal/agent/tools/...
   ```

2. All unit tests pass:
   ```bash
   go test ./internal/agent/tools/... -v -run "TestOkxBatchPlaceOrder|TestOkxBatchCancelOrder|TestOkxGetOrderHistory|TestOkxClosePosition"
   ```

3. Full test suite green:
   ```bash
   go test ./internal/agent/... -v
   ```
</verification>

<success_criteria>
1. Four Tool files created in `internal/agent/tools/`
2. Batch tools enforce 20-order limit
3. Batch tools handle partial failures (separate success/failure tables)
4. Order history supports time range filtering
5. Close position supports percentage-based partial close
6. All tools have rate limiting at 5 req/s
7. All tools validate sCode/sMsg
8. All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-execution-automation/03-04-SUMMARY.md` documenting:
- Tools implemented
- Test coverage
- Any issues encountered
</output>
