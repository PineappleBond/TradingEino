---
phase: 03-execution-automation
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - internal/agent/executor_agent/executor_agent.go
  - internal/agent/executor_agent/DESCRIPTION.md
  - internal/agent/agents.go
autonomous: false
requirements: [EXEC-04]

must_haves:
  truths:
    - "Executor Agent executes trades only on explicit OKXWatcher commands (Level 1 autonomy)"
    - "Executor Agent does not initiate trades independently"
    - "Executor Agent has access to all order management tools"
    - "Executor Agent integrated into AgentsModel"
  artifacts:
    - path: "internal/agent/executor_agent/executor_agent.go"
      provides: "ExecutorAgent initialization function"
      exports: ["NewExecutorAgent"]
    - path: "internal/agent/executor_agent/DESCRIPTION.md"
      provides: "Agent description and capabilities"
      contains: "Level 1 autonomy, execution-only role"
    - path: "internal/agent/agents.go"
      provides: "AgentsModel with Executor field"
      contains: "Executor Agent initialization"
  key_links:
    - from: "internal/agent/executor_agent/executor_agent.go"
      to: "internal/agent/tools/*.go"
      via: "ToolsConfig"
      pattern: "ToolsConfig.*Tools.*Okx.*Tool"
    - from: "internal/agent/agents.go"
      to: "internal/agent/executor_agent/executor_agent.go"
      via: "NewExecutorAgent call"
      pattern: "executor_agent\\.NewExecutorAgent"
---

<objective>
Create Executor Agent with Level 1 autonomy — executes trades only on explicit OKXWatcher commands.

Purpose: Enable autonomous trade execution while maintaining strict control — Executor is a "dumb executor" that follows orders without independent analysis.

Output: ExecutorAgent (ChatModelAgent) with proper prompt constraints, integrated into AgentsModel.
</objective>

<execution_context>
@/Users/leichujun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leichujun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-execution-automation/03-CONTEXT.md
@.planning/phases/03-execution-automation/03-RESEARCH.md

@internal/agent/agents.go
@internal/agent/okx_watcher/okx_watcher.go
@internal/agent/risk_officer/risk_officer.go
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/agent/agents.go:
```go
type AgentsModel struct {
    svcCtx           *svc.ServiceContext
    OkxWatcher       adk.Agent
    RiskOfficer      adk.Agent
    SentimentAnalyst adk.Agent
    // Add: Executor adk.Agent
    mux    sync.Mutex
    ctx    context.Context
    cancel context.CancelFunc
}
```

From internal/agent/okx_watcher/okx_watcher.go:
```go
func NewOkxWatcherAgent(ctx context.Context, svcCtx *svc.ServiceContext, subAgents ...adk.Agent) (*OkxWatcherAgent, error)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ExecutorAgent ChatModelAgent</name>
  <files>internal/agent/executor_agent/executor_agent.go, internal/agent/executor_agent/DESCRIPTION.md</files>
  <action>
    Create Executor Agent as ChatModelAgent with Level 1 autonomy constraints:

    1. Create directory `internal/agent/executor_agent/`

    2. Create `DESCRIPTION.md` documenting:
       - Agent name: ExecutorAgent (Trading Executor)
       - Role: Execute trades on explicit OKXWatcher commands
       - Autonomy Level: 1 (explicit commands only)
       - Capabilities: Place orders, cancel orders, query order status, attach SL/TP, close positions
       - Constraints: DO NOT initiate trades, WAIT for explicit command, REPORT failures without retry

    3. Create `executor_agent.go` with:
       - `NewExecutorAgent(ctx, svcCtx)` function returning `*adk.ChatModelAgent`
       - Use `adk.NewChatModelAgent(ctx, &adk.ChatModelAgentConfig{...})`
       - Set Name: "ExecutorAgent"
       - Set Instruction with Level 1 constraints:
         * "你是交易执行代理，只负责执行 OKXWatcher 的明确指令"
         * "DO NOT initiate trades independently"
         * "WAIT for explicit command from OKXWatcher before executing any trade"
         * "REPORT order failures with full error details to OKXWatcher"
         * "DO NOT retry failed orders unless OKXWatcher explicitly commands retry"
         * "You have access to: okx-place-order, okx-cancel-order, okx-get-order, okx-attach-sl-tp, okx-place-order-with-sl-tp, okx-batch-place-order, okx-batch-cancel-order, okx-close-position"
       - Configure ToolsConfig with all order tools from Plan 01, 02, and 04

    4. Follow sync.Once pattern from agents.go for singleton initialization
  </action>
  <verify>
    <automated>go build ./internal/agent/executor_agent/...</automated>
  </verify>
  <done>ExecutorAgent created as ChatModelAgent with proper prompt constraints</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Executor into AgentsModel</name>
  <files>internal/agent/agents.go</files>
  <action>
    Update `internal/agent/agents.go` to include Executor Agent:

    1. Add `Executor adk.Agent` field to `AgentsModel` struct

    2. In `InitAgents`, after initializing OKXWatcher:
       - Call `executorAgent, err := executor_agent.NewExecutorAgent(ctx, svcCtx)`
       - Handle error: if err != nil, cancel() and return

    3. Set `_agents.Executor = executorAgent.Agent()` in the AgentsModel assignment

    4. Ensure context propagation: pass the same `ctx` from InitAgents to NewExecutorAgent

    5. No changes to Close() method needed — Executor uses same context lifecycle
  </action>
  <verify>
    <automated>go build ./internal/agent/...</automated>
  </verify>
  <done>AgentsModel updated with Executor field, InitAgents calls NewExecutorAgent</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Executor Agent Level 1 Autonomy</name>
  <files>internal/agent/executor_agent/executor_agent.go, internal/agent/agents.go</files>
  <action>
    Manual verification of Executor Agent behavior:

    1. Start the server: `go run cmd/server/main.go`

    2. Test 1 — Direct command to Executor (should refuse):
       - Send query directly to Executor Agent: "Place a buy order for ETH-USDT-SWAP"
       - Expected: Executor should refuse, stating it requires OKXWatcher command

    3. Test 2 — OKXWatcher command flow (should execute):
       - Send query to OKXWatcher: "Analyze ETH-USDT-SWAP and execute if favorable"
       - Expected: OKXWatcher analyzes, then commands Executor to place order
       - Executor should execute the order via OKXWatcher command

    4. Test 3 — Failure reporting:
       - Trigger an order failure (e.g., insufficient balance)
       - Expected: Executor reports failure details, does NOT auto-retry
  </action>
  <verify>
    Human confirms Level 1 autonomy behavior via manual testing
  </verify>
  <done>User types "approved" after verifying Executor enforces Level 1 autonomy</done>
  <what-built>
    - ExecutorAgent implemented as ChatModelAgent
    - Prompt constraints enforcing Level 1 autonomy
    - Integration into AgentsModel
    - Access to all order tools
  </what-built>
  <how-to-verify>
    **Manual verification required for agent behavior:**

    1. Start the server: `go run cmd/server/main.go`

    2. Test 1 — Direct command to Executor (should refuse):
       - Send query directly to Executor Agent: "Place a buy order for ETH-USDT-SWAP"
       - Expected: Executor should refuse, stating it requires OKXWatcher command

    3. Test 2 — OKXWatcher command flow (should execute):
       - Send query to OKXWatcher: "Analyze ETH-USDT-SWAP and execute if favorable"
       - Expected: OKXWatcher analyzes, then commands Executor to place order
       - Executor should execute the order via OKXWatcher command

    4. Test 3 — Failure reporting:
       - Trigger an order failure (e.g., insufficient balance)
       - Expected: Executor reports failure details, does NOT auto-retry
  </how-to-verify>
  <resume-signal>Type "approved" if Executor correctly enforces Level 1 autonomy, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
**Wave 2 Verification (after all tasks complete):**

1. Executor agent compiles:
   ```bash
   go build ./internal/agent/executor_agent/...
   ```

2. Full agent package compiles:
   ```bash
   go build ./internal/agent/...
   ```

3. Manual verification checkpoint passed (Task 3)
</verification>

<success_criteria>
1. ExecutorAgent directory created with executor_agent.go and DESCRIPTION.md
2. ExecutorAgent implemented as ChatModelAgent (not DeepAgent)
3. Prompt constraints explicitly enforce Level 1 autonomy
4. AgentsModel updated with Executor field
5. InitAgents initializes Executor via NewExecutorAgent
6. Human verification confirms Level 1 behavior (refuses direct commands, executes OKXWatcher commands)
</success_criteria>

<output>
After completion, create `.planning/phases/03-execution-automation/03-03-SUMMARY.md` documenting:
- ExecutorAgent implementation
- Prompt constraints for Level 1 autonomy
- Manual verification results
- Any issues encountered
</output>
